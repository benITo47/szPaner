# Development zone - typical coding setup
zone "dev" {
    # Main editor pane - takes up the left side
    pane "editor" {
        execute "echo 'Ready for nvim/vim'"
        size 60%
    }

    # Server pane - top right
    pane "server" {
        execute "echo 'npm run dev goes here'"
        split "right"
        size 40%
    }

    # Logs pane - bottom right
    pane "logs" {
        execute "echo 'tail -f logs/app.log'"
        split "down"
    }
}

# Server monitoring zone - SSH is just a execute!
zone "servers" {
    pane "prod" {
        execute "ssh user@prod.example.com"
        size 50%
    }

    pane "staging" {
        execute "ssh user@staging.example.com"
        split "right"
        size 50%
    }

    pane "local" {
        execute "htop"
        split "down"
    }
}
